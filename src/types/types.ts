export interface Inputs {
  account: string;
  sum: number;
  category: string;
  counterParty: string;
  date: string;
}

export interface IAccounts {
  id: string;
  account: string;
  sum: number;
}
